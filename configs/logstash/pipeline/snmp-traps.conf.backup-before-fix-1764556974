input {
  snmptrap {
    host => "0.0.0.0"
    port => 1062
    community => ["public"]
  }
}

filter {
  # STEP 1: Map source IP to hostname
  if [host] == "172.20.20.23" {
    mutate { 
      add_field => { "host.name" => "csr23" }
    }
  } else if [host] == "172.20.20.24" {
    mutate { 
      add_field => { "host.name" => "csr24" }
    }
  } else if [host] == "172.20.20.25" {
    mutate { 
      add_field => { "host.name" => "csr25" }
    }
  } else if [host] == "172.20.20.26" {
    mutate { 
      add_field => { "host.name" => "csr26" }
    }
  } else if [host] == "172.20.20.27" {
    mutate { 
      add_field => { "host.name" => "csr27" }
    }
  } else if [host] == "172.20.20.28" {
    mutate { 
      add_field => { "host.name" => "csr28" }
    }
  } else if [host] == "172.20.20.29" {
    mutate { 
      add_field => { "host.name" => "csr29" }
    }
  }

  # STEP 2: Extract and flatten trap OID FIRST
  if [iso][org][dod][internet][snmpV2][snmpModules][snmpMIB][snmpMIBObjects][snmpTrap][snmpTrapOID][0] {
    mutate {
      add_field => {
        "trap.oid" => "%{[iso][org][dod][internet][snmpV2][snmpModules][snmpMIB][snmpMIBObjects][snmpTrap][snmpTrapOID][0]}"
      }
    }
  }

  # STEP 3: Classify trap types using trap.oid
  if [trap.oid] == "1.3.6.1.6.3.1.1.5.3" {
    # Link Down
    mutate { 
      add_tag => ["interface_down"]
      add_field => { 
        "event.action" => "interface-down"
        "event.kind" => "alert"
        "event.category" => ["network"]
        "event.type" => ["change", "end"]
        "event.severity" => 3
        "message" => "Interface down on %{[host.name]}"
      }
    }
  }

  if [trap.oid] == "1.3.6.1.6.3.1.1.5.4" {
    # Link Up
    mutate { 
      add_tag => ["interface_up"]
      add_field => { 
        "event.action" => "interface-up"
        "event.kind" => "event"
        "event.category" => ["network"]
        "event.type" => ["change", "start"]
        "event.severity" => 1
        "message" => "Interface up on %{[host.name]}"
      }
    }
  }

  if [trap.oid] == "1.0.8802.1.1.2.0.0.1" {
    # LLDP Topology Change
    mutate {
      add_tag => ["lldp_change"]
      add_field => {
        "event.action" => "lldp-topology-change"
        "event.kind" => "event"
        "event.category" => ["network"]
        "event.type" => ["info"]
        "event.severity" => 2
        "message" => "LLDP topology change on %{[host.name]}"
      }
    }
  }

  # STEP 4: Data stream routing
  mutate {
    add_field => {
      "data_stream.type" => "logs"
      "data_stream.dataset" => "snmp.trap"
      "data_stream.namespace" => "prod"
    }
  }
}

output {
  stdout { 
    codec => rubydebug 
  }
  
  elasticsearch {
    hosts => ["https://gcp-telco-9-3-55a713.es.us-west2.gcp.elastic-cloud.com:443"]
    api_key => "RzhSdXc1b0I4Y3k0Mlh5WG91WWE6UEI0blZXNk85Yk80R2lURXBzNllEZw=="
    data_stream => true
    data_stream_type => "logs"
    data_stream_dataset => "snmp.trap"
    data_stream_namespace => "prod"
  }
}
